name: test

channels:
  - conda-forge

dependencies:
  # Base depends
  - python
  - pip

  # SEAMM
  - seamm

  - geometric
  - numpy
  - rdkit
  - tabulate

  # Testing
  - black
  - codecov
  - flake8
  - pytest
  - pytest-cov

  # Documentation
  - pydata-sphinx-theme
  - sphinx-design

  # Pip-only installs
  - pip:
    - read-structure-step
    
    # Documentation
    - sphinx-copybutton
    - sphinxnotes-strike
